@model IEnumerable<BookStoreOnline.Models.Category>

@{
    ViewBag.Title = "Index";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}
<div class="card-body">
    <h2>Các thể loại sách</h2>
</div>
<div class="text-right">
    <button type="button" class="btn btn-primary" onclick="location.href='/Admin/Categories/Create';">Thêm thể loại mới</button>
</div>
<div class="card-body">
    <div class="table-responsive">
        <table class="table table-striped">
            <tr>
                <th>
                    Thể loại
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.CategoryName)
                    </td>
                    <td >
                        <a href="@Url.Action("Edit", new { id = item.CategoryID})" class="btn btn-sm btn-warning">Chỉnh sửa</a> |
                        <a href="@Url.Action("Delete", new { id = item.CategoryID })" class="btn btn-sm btn-danger">Xóa</a>
                    </td>
                </tr>
            }

        </table>
    </div>
</div>
